# TechDocs 작업 하네스

이 문서는 TechDocs 저장소 전체에 적용되는 최상위 작업 규칙이다. 사람과 AI는 코드 또는 문서를 수정하기 전에 이 문서와 `Docs/harness/`의 세부 규칙을 확인한다.

## 필수 확인 문서

1. 코딩, 이름 및 주석 규칙: `Docs/harness/CODING_RULES.md`
2. 브랜치, 커밋 및 PR 규칙: `Docs/harness/GIT_RULES.md`
3. PR 본문 형식: `.github/pull_request_template.md`

## 작업 원칙

- 요청과 관계없는 파일을 수정하지 않는다.
- 수정 전에 관련 코드, 호출부, 테스트 및 설정을 확인한다.
- 공개 API, JSON 필드, HTTP 상태, NDJSON 및 SSE 형식을 임의로 변경하지 않는다.
- `.env`, API 키, 토큰 및 개인정보를 코드, 로그, 문서에 포함하지 않는다.
- 실패를 빈 `catch`, 빈 `except`, 더미 성공값 또는 과도한 fallback으로 숨기지 않는다.
- 코드 변경 후 영향 범위에 맞는 테스트, 린트 또는 빌드를 실행한다.
- 실행하지 못한 검증은 완료된 것처럼 쓰지 않고 이유를 PR에 남긴다.
- Next.js 작업 전에는 설치된 `frontend/node_modules/next/dist/docs/`의 관련 문서를 확인한다.

## 파일 상단 주석

새로 만들거나 의미 있게 수정하는 직접 관리 소스 파일에는 다음 형식의 설명을 파일 상단에 작성한다.

```text
// ============================================================
// 파일 역할: 파일이 담당하는 역할을 한 문장으로 설명한다.
//
// 작성자: 심우현
// 최종 수정일: 2026년 8월 10일
//
// 주요 책임:
// - 핵심 책임 1
// - 핵심 책임 2
// ============================================================
```

- Python은 `#`, CSS는 `/* ... */`처럼 언어에 맞는 주석 문법을 사용한다.
- 복잡한 로직은 무엇을 하는지 반복하지 않고 왜 필요한지를 한국어로 설명한다.
- 로직 또는 파일 책임이 변경되면 최종 수정일과 설명을 함께 갱신한다.
- JSON, lock 파일, 자동 생성 파일, 마이그레이션 및 외부 코드는 적용 대상에서 제외한다.

## Git 작업 흐름

- `main`에 직접 커밋하거나 직접 푸시하지 않는다.
- 모든 변경은 별도 작업 브랜치에서 진행한다.
- 검증된 변경만 작업 브랜치에 커밋하고 원격 브랜치에 푸시한다.
- `main` 대상 PR을 만들고 CI 결과를 확인한 후 병합한다.
- 사용자가 병합까지 요청한 경우에만 AI가 PR을 병합한다.
- 임시 PR 본문을 `.pr-body.md` 같은 파일로 저장하거나 커밋하지 않는다.

## 기본 검증

```bash
# Backend
cd backend && python -m compileall -q app
cd backend && python -m pytest

# Frontend
cd frontend && npm run lint
cd frontend && npx tsc --noEmit
cd frontend && npm run build
```

## 문서 관리

- 하네스 문서는 `Docs/harness/`에서 관리한다.
- 새 Markdown 문서를 추가하거나 이동 또는 삭제하면 루트 `README.md`의 Markdown 문서 지도를 함께 갱신한다.
- 일회성 작업 기록, 과거 계획, 생성된 평가 결과 및 임시 PR 본문을 Markdown으로 커밋하지 않는다.
